# fs-app

Cross-platform (iOS, Android) commands and utilities for the Foodshare app. This crate powers the `fs-app` CLI and contains platform-specific functionality for iOS (Xcode, Simulator, Swift, IPAs) and Android (Gradle, Emulators, Kotlin, APKs).
